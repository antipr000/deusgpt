import { ChatHeader } from "@lobehub/ui";

const Navbar = () => {
  return (
    <ChatHeader
      style={{
        height: "75px",
        position: "unset",
      }}
    />
  );
};

export default Navbar;
